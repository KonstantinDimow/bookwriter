<h1>VersionsVergleich</h1>

<table class="table table-striped max700">
  <tr>
    <th>version1</th>
    <th>version2</th>
  </tr>

  <tr>
     <td><%= raw(@version1) %></td>
     <td><%= raw(@version2) %></td>
  </tr>

</table>

<%= button_to 'Zurück', book_chunk_version_path(@book,@chunk.id), method: :get, :class => 'btn btn-default backbutton' %>